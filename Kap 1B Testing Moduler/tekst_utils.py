def forekomstAvBokstav(tekst, bokstav):
    ant = 0
    for b in tekst:
        if bokstav == b:
            ant += 1
    return ant

def antallOrd(tekst):
    # Teller antall ord i en tekst, 
    # og returnerer det (int)
    pass